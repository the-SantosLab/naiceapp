// Shared app-identity and signing defaults
DEVELOPMENT_TEAM = GX89CS2T99
PRODUCT_BUNDLE_IDENTIFIER = pro.naice.app
APP_BUNDLE_IDENTIFIER = pro.naice.app
MARKETING_VERSION = 1.0

// Optional per-machine overrides
#include? "Local.xcconfig"